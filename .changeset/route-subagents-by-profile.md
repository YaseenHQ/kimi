---
"@moonshot-ai/agent-core": minor
"@moonshot-ai/agent-core-v2": minor
"@moonshot-ai/kimi-code": minor
---

Allow custom agent profiles to select a model for delegated work. Set `model` in an agent file, or use `inherit` to keep the caller's model.
